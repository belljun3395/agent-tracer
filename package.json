{
  "name": "agent-tracer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "lint:fix": "npm run lint:fix --workspaces --if-present",
    "lint:deps": "depcruise --config .dependency-cruiser.cjs packages",
    "test": "npm run test --workspaces --if-present",
    "docs:dev": "vitepress dev docs --host 0.0.0.0 --port 5174",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs --host 0.0.0.0 --port 4174",
    "dev": "concurrently -k -n server,web \"npm run dev:server\" \"npm run dev:web\"",
    "dev:server": "NODE_OPTIONS=\"--conditions=development --import @swc-node/register/esm-register\" SWC_NODE_PROJECT=packages/server/tsconfig.dev.json node --watch packages/server/src/server.entry.ts",
    "dev:web": "npm run dev --workspace @monitor/web -- --host 0.0.0.0",
    "seed": "NODE_OPTIONS=\"--conditions=development --import @swc-node/register/esm-register\" SWC_NODE_PROJECT=packages/server/tsconfig.dev.json node packages/server/src/main/seed.ts",
    "start:server": "node packages/server/dist/index.js",
    "codex:observe": "npm --prefix packages/runtime run codex:observe --",
    "setup:external": "node scripts/setup-external.mjs",
    "setup:statusline": "bash scripts/install-statusline.sh",
    "publish:all": "npm publish --workspace @monitor/server --access public"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "@swc-node/register": "^1.11.1",
    "concurrently": "^9.0.0",
    "dependency-cruiser": "^16.10.4",
    "eslint": "^9.0.0",
    "eslint-plugin-n": "^17.24.0",
    "globals": "^15.0.0",
    "mermaid": "^11.12.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0",
    "typescript-eslint": "^8.0.0",
    "vitepress": "^2.0.0-alpha.17"
  }
}
