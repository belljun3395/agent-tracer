# Agent Tracer Plugin — Data Flow

This document describes how each hook handler processes data: what fields come from
Claude Code on stdin, what the plugin generates itself, and what gets sent to the
Agent Tracer monitor API.

---

## Architecture overview

```
Claude Code                  Plugin (tsx)                  Monitor API
──────────────────────────────────────────────────────────────────────
  event fires
      │
      ▼
  run-hook.sh ──────────── hooks/<handler>.ts
                               │
                               ├─ stdin: JSON payload (from Claude Code)
                               │
                               ├─ reads/writes: transcript cursor only
                               │                (.claude/.transcript-cursors/<sid>.json)
                               │
                               ├─ generates: titles, bodies, ids
                               │
                               └─ POST ──────────────────────────────► /api/* /ingest/v1/events
```

Each hook process is short-lived. As of plugin v0.2.0 the plugin no longer
maintains per-session FS caches — every handler re-calls
`/api/runtime-session-ensure` (idempotent on the server). The only plugin-local
state is the transcript cursor, which tracks byte offsets into Claude Code's
append-only transcript JSONL so Stop/SubagentStop can tail only new entries.

> **Migration note:** Plugin versions < 0.2.0 wrote to
> `.claude/.session-cache/` and `~/.claude/.session-history.json`. As of
> v0.2.0 those files are no longer read or written. Safe to delete any
> existing copies. The transcript cursor moved from
> `.claude/.session-cache/<sid>-transcript-cursor.json` to
> `.claude/.transcript-cursors/<sid>.json`; the plugin reads the legacy
> path once on upgrade and drains it on the first successful new-location write.

---

## Data origin legend

| Symbol | Meaning |
|--------|---------|
| **[CC]** | Value comes directly from Claude Code's hook payload (stdin) |
| **[GEN]** | Value is generated by the plugin at runtime |
| **[ENSURE]** | Value is returned from `/api/runtime-session-ensure` (re-called each hook; server is idempotent) |
| **[ENV]** | Value comes from an environment variable |
| **[TX]** | Value is parsed from the Claude Code session transcript JSONL (`transcript_path`) |

---

## Semantic metadata

The plugin classifies each event at the hook level using the shared inference
library (`shared/semantics/inference.*`). Each PostToolUse handler calls the
appropriate `infer*Semantic()` function to derive `lane`, `subtypeKey`,
`subtypeLabel`, `subtypeGroup`, `toolFamily`, `operation`, and
`entityType`/`entityName`/`sourceTool` — all included in the ingest payload
alongside handler-specific fields.

Two additional layers are applied before every `postTaggedEvent` call:
- `provenEvidence(reason)` stamps `evidenceLevel: "proven"` and `evidenceReason`
- `withTags(meta)` derives normalised `tags` from the full metadata object

Each section below lists the complete metadata block for that handler.
The shared semantic fields are shown explicitly for Bash (§7) and abbreviated
as `# … shared semantic block …` in subsequent sections.

---

## Session lifecycle

### 1. SessionStart → `SessionStart.ts`

**Claude Code provides [CC]:**
```
session_id        — unique session identifier
source            — "startup" | "resume" | "clear" | "compact"
cwd               — current working directory
model             — model in use (e.g. "claude-sonnet-4-6")
hook_event_name   — "SessionStart"
```

**Plugin generates [GEN]:**
```
title   — derived from source: "Session started", "Session resumed", etc.
body    — derived from source: human-readable description
trigger — same as source, stored as metadata
```

**Plugin writes to disk:** none. The server records the start timestamp from
the `/api/runtime-session-ensure` call; the plugin no longer persists session
metadata locally.

**API calls:**
```
POST /api/runtime-session-ensure
  runtimeSource     [GEN] "claude-plugin"
  runtimeSessionId  [CC]  session_id
  title             [GEN] defaultTaskTitle() = "Claude Code — <basename>"
  workspacePath     [ENV] $CLAUDE_PROJECT_DIR

POST /ingest/v1/events  { kind: "context.saved", … }
  taskId    [ENSURE] from ensureRuntimeSession response
  sessionId [ENSURE] from ensureRuntimeSession response
  title     [GEN]   "Session started" / "Session resumed" / …
  body      [GEN]   description text
  lane      [GEN]   "planning"
  metadata:
    trigger [CC]    source ("startup" | "resume" | "clear" | "compact")
```

---

### 2. SessionEnd → `SessionEnd.ts`

**Claude Code provides [CC]:**
```
session_id   — same session_id from SessionStart
reason       — "prompt_input_exit" | "resume" | "logout" | "clear" | …
cwd          — current working directory
```

**Plugin generates [GEN]:**
```
completionReason  — "explicit_exit" (prompt_input_exit) or "runtime_terminated"
endedAt           — Date.now() at time of hook (included in session.ended metadata)
```

**Plugin writes to disk:** none. The transcript cursor for this session is
deleted (see cleanup below) but no session records are persisted locally.

**Plugin cleans up:**
```
.claude/.transcript-cursors/<sessionId>.json  (deleted)
```

**API calls:**
```
POST /api/runtime-session-ensure    # re-ensure to resolve (taskId, sessionId)
  runtimeSource     [GEN]   "claude-plugin"
  runtimeSessionId  [CC]    session_id
  title             [GEN]   defaultTaskTitle()
  workspacePath     [ENV]   $CLAUDE_PROJECT_DIR

POST /api/runtime-session-end
  runtimeSource     [GEN]   "claude-plugin"
  runtimeSessionId  [CC]    session_id
  summary           [GEN]   "Claude Code session ended (<reason>)"
  completionReason  [GEN]   "explicit_exit" | "runtime_terminated"
  (no metadata object)

POST /ingest/v1/events  { kind: "session.ended", … }
  taskId    [ENSURE] from runtime-session-ensure
  sessionId [ENSURE] from runtime-session-ensure
  title     [GEN]   "Session ended (<reason-description>)"
  body      [GEN]   "Claude Code session ended (<reason>)."
  lane      [GEN]   "user"
  metadata:
    reason            [CC]   payload.reason
    completionReason  [GEN]  mapped from reason
    source            [GEN]  "session-end"
    sessionEndedAt    [GEN]  ISO timestamp of endedAt
    transcriptPath    [CC]   payload.transcript_path (omitted if absent)
    permissionMode    [CC]   payload.permission_mode (omitted if absent)
    cwd               [CC]   payload.cwd             (omitted if absent)
```

> `clear` reason is skipped — SessionStart handles the new session that immediately
> follows a /clear command.

---

### 3. Session resume flow

Session history is owned by the server as of plugin v0.2.0. The plugin no
longer writes `~/.claude/.session-history.json`; `runtime-session-ensure` is
idempotent on `runtimeSessionId`, so reattaching to an existing task is a
matter of calling ensure again with the original session ID.

External tools that need to enumerate past sessions query the server's
lifecycle events (`session.started` / `session.ended`) instead of reading a
local history file.

---

## Per-turn lifecycle

### 4. UserPromptSubmit → `UserPromptSubmit.ts`

**Claude Code provides [CC]:**
```
session_id   — unique session identifier
prompt       — raw text the user typed
```

**Plugin generates [GEN]:**
```
messageId  — crypto.randomUUID()
title      — first 120 chars of prompt (or defaultTaskTitle if empty)
phase      — "initial" (first message, task just created) | "follow_up"
captureMode — "raw"
source      — "claude-plugin"
```

**API calls:**
```
POST /api/runtime-session-ensure  (ensures task exists before posting the message)

POST /api/user-message
  taskId      [ENSURE] from ensureRuntimeSession
  sessionId   [ENSURE]
  messageId   [GEN]   UUID
  phase       [GEN]   "initial" | "follow_up"
  title       [GEN]   truncated prompt (max 120 chars)
  body        [CC]    prompt (full text)
  source      [GEN]   "claude-plugin"
  captureMode [GEN]   "raw"
  (no metadata object)
```

---

### 5. Stop → `Stop.ts`

**Claude Code provides [CC]:**
```
session_id              — unique session identifier
transcript_path         — path to the session transcript JSONL
last_assistant_message  — final text of Claude's response
agent_id                — set when inside a subagent (routes response to the child task)
```

**Plugin reads from transcript JSONL [TX]:**
```
message.stop_reason  — "end_turn" | "tool_use" | "max_tokens" | …
```
> Token usage (`input_tokens`, `output_tokens`, cache counters) is **not read
> here**. It is collected independently via the OTLP exporter (see §15).

**Plugin generates [GEN]:**
```
messageId  — crypto.randomUUID()
title      — first 120 chars of last_assistant_message (or "Response (<stop_reason>)")
source     — "claude-plugin"
```

**API calls:**
```
POST /ingest/v1/events                          (one assistant.response event)
  kind      [GEN]   "assistant.response"
  taskId    [ENSURE] from resolveEventSessionIds
  sessionId [ENSURE]
  messageId [GEN]   UUID
  title     [GEN]   truncated response text
  body      [CC]    last_assistant_message
  source    [GEN]   "claude-plugin"
  metadata:
    stopReason  [TX]  message.stop_reason

POST /ingest/v1/events                          (transcript tail batch — see §14)
POST /api/runtime-session-end                    (skipped when agent_id is present)
```

---

## Tool event lifecycle

### 6. PreToolUse (all tools) → `PreToolUse.ts`

Runs before **every** tool call. Its sole job is to guarantee that a task and session
exist in the monitor before any PostToolUse event tries to post against them.

**Claude Code provides [CC]:**
```
session_id  — unique session identifier
tool_name   — name of the tool about to run
agent_id    — set when running inside a subagent
```

**Two paths:**

**Path A — subagent event (agent_id is present):**
```
POST /api/runtime-session-ensure
  runtimeSessionId  [GEN]   "sub--<agent_id>"
  parentTaskId      [ENSURE] parent task ID (resolved from parent session_id)
  parentSessionId   [ENSURE] parent monitor session ID
  (server dedupes on runtimeSessionId — idempotent)
```

**Path B — normal session (no agent_id):**
```
POST /api/runtime-session-ensure
  runtimeSessionId  [CC]  session_id
  title             [GEN] "Claude Code — <project basename>"
  (server dedupes on runtimeSessionId — idempotent)
```

---

### 7. PostToolUse: Bash → `PostToolUse/Bash.ts`

**Claude Code provides [CC]:**
```
session_id             — unique session identifier
tool_name              — "Bash"
tool_input.command     — the shell command that ran
tool_input.description — human-readable description (optional)
```

**Plugin generates [GEN]:**
```
title     — description || first 80 chars of command
body      — "description\n\n$ command" or just command
```

**API call:**
```
POST /ingest/v1/events  { kind: "terminal.command", … }
  taskId    [ENSURE]
  sessionId [ENSURE]
  lane      [GEN]   "exploration" | "implementation"
  title     [GEN]
  body      [GEN]
  metadata:
    evidenceLevel  [GEN]  "proven"
    evidenceReason [GEN]  "Observed directly by the Bash PostToolUse hook."
    subtypeKey     [GEN]  "shell_probe" | "run_test" | "run_lint" | "run_build" | "verify" | "rule_check" | "run_command"
    subtypeLabel   [GEN]  human-readable label for subtypeKey
    subtypeGroup   [GEN]  "shell" | "execution"
    toolFamily     [GEN]  "terminal"
    operation      [GEN]  "probe" | "execute"
    entityType     [GEN]  "command"
    entityName     [GEN]  first token of the command string
    sourceTool     [GEN]  "Bash"
    tags           [GEN]  normalised tag array (via withTags)
    command        [CC]   tool_input.command
    description    [CC]   tool_input.description  (omitted if absent)
    toolUseId      [CC]   tool_use_id             (omitted if absent)
```

---

### 8. PostToolUse: Edit | Write → `PostToolUse/File.ts`

**Claude Code provides [CC]:**
```
session_id             — unique session identifier
tool_name              — "Edit" | "Write"
tool_input.file_path   — target file path
tool_input.content     — (Write) full file content
tool_input.old_string  — (Edit) text being replaced
tool_input.new_string  — (Edit) replacement text
```

**Plugin generates [GEN]:**
```
title    — "<toolName>: <filename>"  (basename of file_path)
body     — "Modified <relPath>"
relPath  — file_path relative to $CLAUDE_PROJECT_DIR
```

**API call:**
```
POST /ingest/v1/events  { kind: "tool.used", … }
  taskId    [ENSURE]
  sessionId [ENSURE]
  lane      [GEN]   "implementation"
  toolName  [CC]    tool_name
  title     [GEN]
  body      [GEN]
  filePaths [CC]    [tool_input.file_path]
  metadata:
    # … shared semantic block (inferFileToolSemantic) …
    # subtypeKey: "create_file" | "modify_file" | "apply_patch" | "rename_file" | "delete_file"
    # toolFamily: "file"  subtypeGroup: "file_ops"
    filePath  [CC]  tool_input.file_path
    relPath   [GEN] path relative to project root
    toolUseId [CC]  tool_use_id  (omitted if absent)
```

---

### 9. PostToolUse: Read | Glob | Grep | WebSearch | WebFetch → `PostToolUse/Explore.ts`

**Claude Code provides [CC]:**
```
session_id    — unique session identifier
tool_name     — "Read" | "Glob" | "Grep" | "WebSearch" | "WebFetch"
tool_input    — tool-specific fields (file_path, pattern, query, url, …)
```

**Plugin generates [GEN]:**
```
title    — tool-specific summary ("Read: filename", "Grep: pattern", …)
body     — human description of the operation
```

**API call:**
```
POST /ingest/v1/events  { kind: "tool.used", … }
  taskId    [ENSURE]
  sessionId [ENSURE]
  lane      [GEN]   "exploration"
  toolName  [CC]    tool_name
  title     [GEN]
  body      [GEN]
  filePaths [CC]    extracted from tool_input (max 300 chars each)
  metadata:
    # … shared semantic block (inferExploreSemantic) …
    # subtypeKey: "read_file" | "glob_files" | "grep_code" | "web_fetch" | "web_search" | "list_files"
    # toolFamily: "explore"  subtypeGroup: "files" | "search" | "web"
    toolInput  [CC]  stringified tool_input object
    webUrls    [CC]  [url | query]  (only for WebFetch/WebSearch)
    toolUseId  [CC]  tool_use_id  (omitted if absent)
```

---

### 10. PostToolUse: Agent | Skill → `PostToolUse/Agent.ts`

**Claude Code provides [CC]:**
```
session_id                 — unique session identifier
tool_name                  — "Agent" | "Skill"
tool_input.prompt          — (Agent) task description
tool_input.description     — (Agent) short description
tool_input.subagent_type   — (Agent) named agent type
tool_input.run_in_background — (Agent) async flag
tool_input.skill           — (Skill) skill name
tool_input.args            — (Skill) arguments
tool_response              — (Agent, background) contains child session_id
```

**Plugin generates [GEN]:**
```
activityType  — "delegation" | "skill_use"
title         — "Agent: <description>" | "Skill: <skillName>"
childTitle    — description | prompt | defaultTaskTitle()
```

**API calls:**
```
POST /ingest/v1/events  { kind: "agent.activity.logged", … }
  taskId       [ENSURE]
  sessionId    [ENSURE]
  lane         [GEN]   "coordination"
  activityType [GEN]   "delegation" | "skill_use"
  title        [GEN]
  body         [CC]    prompt | skill args
  agentName    [CC]    tool_input.subagent_type  (Agent only, omitted if absent)
  skillName    [CC]    tool_input.skill          (Skill only, omitted if absent)
  metadata:
    # … shared semantic block (inferAgentSemantic / inferSkillSemantic) …
    # subtypeKey: "delegation" | "skill_use"  toolFamily: "coordination"
    toolInput  [CC]  stringified tool_input object
    toolUseId  [CC]  tool_use_id               (omitted if absent)

# Background agents only: create child runtime session with parent linkage
POST /api/runtime-session-ensure
  runtimeSessionId [CC]   child session_id parsed from tool_response
  title            [GEN]  childTitle
  parentTaskId     [ENSURE] parent task ID
  parentSessionId  [ENSURE] parent monitor session ID
  (no metadata object)
```

---

### 11. PostToolUse: mcp__* → `PostToolUse/Mcp.ts`

**Claude Code provides [CC]:**
```
session_id  — unique session identifier
tool_name   — "mcp__<server>__<tool>"  (e.g. "mcp__memory__create_entities")
tool_input  — MCP tool arguments
```

**Plugin generates [GEN]:**
```
mcpServer    — parsed from tool_name (e.g. "memory")
mcpTool      — parsed from tool_name (e.g. "create_entities")
title        — "MCP: <server>/<tool>"
body         — "Used MCP tool <server>/<tool>"
activityType — "mcp_call"
```

**API call:**
```
POST /ingest/v1/events  { kind: "agent.activity.logged", … }
  taskId       [ENSURE]
  sessionId    [ENSURE]
  lane         [GEN]   "coordination"
  activityType [GEN]   "mcp_call"
  title        [GEN]
  body         [GEN]
  mcpServer    [GEN]   parsed server name
  mcpTool      [GEN]   parsed tool name
  metadata:
    # … shared semantic block (inferMcpSemantic) …
    # subtypeKey: "mcp_call"  toolFamily: "coordination"  entityName: "{mcpServer}/{mcpTool}"
    mcpServer  [GEN]  parsed server name
    mcpTool    [GEN]  parsed tool name
    toolUseId  [CC]   tool_use_id  (omitted if absent)
```

> **Self-reference guard**: MCP calls to `mcp__agent-tracer__*` are skipped to prevent
> the monitor itself from generating recursive monitoring events.

---

### 12. PostToolUse: TaskCreate | TaskUpdate | TodoWrite → `PostToolUse/Todo/Todo.ts`

**Claude Code provides [CC]:**
```
session_id   — unique session identifier
tool_name    — "TaskCreate" | "TaskUpdate" | "TodoWrite"
tool_input   — tool-specific fields
  TodoWrite:   todos[].{ content, status, priority }
  TaskCreate:  task_subject, task_description
  TaskUpdate:  task_id, status
```

**Plugin generates [GEN]:**
```
todoId    — SHA-1(content + "::" + priority).slice(0,16)  (stable, content-addressed)
           — or task_id for TaskCreate/TaskUpdate
todoState — "added" | "in_progress" | "completed" | "cancelled"  (mapped from status)
```

**API call (one per todo item):**
```
POST /api/todo
  taskId    [ENSURE]
  sessionId [ENSURE]
  todoId    [GEN]   stable content-hash or task_id
  todoState [GEN]   mapped from status field
  title     [CC]    todo content / task_subject
  metadata:
    priority  [CC]  todo priority (omitted for TaskCreate/TaskUpdate)
    status    [CC]  original status string
    toolName  [CC]  "TaskCreate" | "TaskUpdate" | "TodoWrite"
    toolUseId [CC]  tool_use_id  (omitted if absent)
```

---

### 13. PostToolUseFailure (all matched tools) → `PostToolUseFailure.ts`

Fires when any matched tool fails. Matcher: `Bash|Edit|Write|Agent|Skill|TaskCreate|TaskUpdate|TodoWrite|mcp__.*`

**Claude Code provides [CC]:**
```
session_id   — unique session identifier
tool_name    — name of the tool that failed
tool_input   — the tool's input parameters
error        — error message string
is_interrupt — boolean: true if user interrupted the tool
```

**Plugin generates [GEN]:**
```
title  — "Failed <toolName>"
body   — error message or "Tool failed: <toolName>"
```

**Two paths:**

**Path A — MCP tool failure:**
```
POST /api/agent-activity
  taskId       [ENSURE]
  sessionId    [ENSURE]
  activityType [GEN]   "mcp_call"
  title        [GEN]   "Failed mcp__<server>__<tool>"
  body         [CC]    error
  lane         [GEN]   "coordination"
  mcpServer    [GEN]   parsed server name
  mcpTool      [GEN]   parsed tool name
  metadata:
    failed      [GEN]  true
    error       [CC]   error message
    isInterrupt [CC]   is_interrupt
    toolUseId   [CC]   tool_use_id  (omitted if absent)
    mcpServer   [GEN]  parsed server name
    mcpTool     [GEN]  parsed tool name
```

**Path B — Non-MCP tool failure (Edit, Write, Agent, Bash, …):**
```
POST /api/tool-used
  taskId    [ENSURE]
  sessionId [ENSURE]
  toolName  [CC]    tool_name
  title     [GEN]   "Failed <toolName>"
  body      [CC]    error message
  lane      [GEN]   "implementation"
  metadata:
    failed      [GEN]  true
    error       [CC]   error message
    isInterrupt [CC]   is_interrupt
    toolUseId   [CC]   tool_use_id  (omitted if absent)
    description [CC]   tool_input.description  (Bash only, omitted if absent)
    + shared semantic fields                   (Bash only: inferred from failed command)
```

---

## Subagent session linking

The plugin tracks subagent sessions with a virtual runtime session ID:
`sub--<agent_id>`.

```
SubagentStart fires → SubagentStart.ts eagerly resolves:
  parent session_id                [CC]
  child runtimeSessionId           [GEN] "sub--<agent_id>"

  POST /api/async-task  (asyncStatus: "running")
    metadata:
      agentId      [CC]    agent_id
      agentType    [CC]    agent_type
      parentTaskId [ENSURE] parent task ID
      childTaskId  [ENSURE] child task ID

Next PreToolUse / PostToolUse inside the subagent fires:
  resolveEventSessionIds(...)
  → resolveSubagentSessionIds(...)
  → POST /api/runtime-session-ensure (idempotent — server returns same child IDs)

SubagentStop fires:
  POST /api/async-task  (asyncStatus: "completed")
    metadata:
      agentId      [CC]    agent_id
      agentType    [CC]    agent_type
      parentTaskId [ENSURE] parent task ID
```

This eager child-session creation keeps the subagent's task linked to the parent
before any tool events from the child are posted, without needing a separate
registry file.

---

## Compaction lifecycle

### PreCompact / PostCompact → `PreCompact.ts` / `PostCompact.ts`

**Claude Code provides [CC]:**
```
session_id           — unique session identifier
hook_event_name      — "PreCompact" | "PostCompact"
trigger              — "manual" | "auto"
custom_instructions  — (PreCompact, undocumented) instructions before compaction
compact_summary      — (PostCompact, undocumented) generated summary text
```

**API call:**
```
POST /api/save-context
  taskId    [ENSURE]
  sessionId [ENSURE]
  title     [GEN]   "Context compacting" | "Context compacted"
  body      [CC]    custom_instructions | compact_summary
  lane      [GEN]   "planning"
  metadata:
    trigger       [CC]  "manual" | "auto"
    compactPhase  [GEN] "before" (PreCompact) | "after" (PostCompact)
```

---

## 14. Transcript tail lifecycle

Hook payloads carry only shallow `tool_input` / `tool_response` fields. They
cannot see the model's `thinking` blocks, intermediate narration text between
tool calls, or the system-injected attachments that Claude Code threads into
context (task reminders, plan mode, skill listings, tool/MCP deltas, nested
memory). All of that does appear in the session transcript JSONL that Claude
Code writes at `transcript_path`. Stop and SubagentStop tail this file on each
turn boundary and publish the new entries as events.

**Trigger points:**
- `Stop.ts` tails the parent session transcript (`transcript_path`).
- `SubagentStop.ts` tails the child session transcript (`agent_transcript_path`)
  and publishes events onto the child task via `resolveEventSessionIds`.

**Cursor file (per logical session):**
```
<PROJECT_DIR>/.claude/.transcript-cursors/<sessionId>.json
  { lastEmittedUuid: string | null, byteOffset: number, fileSize: number }
```
(Legacy location `.session-cache/<sessionId>-transcript-cursor.json` is read
as a one-time migration fallback and deleted after the first new-location write.)
- Parent sessions use the real session_id.
- Subagents use the virtual ID `sub--<agent_id>` so sibling agents stay
  isolated and cursors are deleted with the agent lifecycle.

**Read algorithm:**
1. `fs.statSync` the transcript. If `fileSize < cursor.fileSize` (truncate,
   compact) reset the cursor to `null`.
2. Parse all lines (malformed lines are skipped).
3. Locate `lastEmittedUuid` by UUID equality. Everything after it is new.
4. After successful post, save `{ lastEmittedUuid = last entry's uuid,
   byteOffset = fileSize, fileSize }`.

**Entry → event mapping:**

| Transcript entry | Ingest kind | Lane | Notes |
|---|---|---|---|
| `assistant.message.content[].type === "thinking"` | `thought.logged` | (default) | body is the thinking text |
| `assistant.message.content[].type === "text"` when the same message also carries a `tool_use` block | `assistant.response` with `metadata.phase = "intermediate"` | (default) | intermediate narration; final-turn text comes from the primary `/api/assistant-response` post |
| `attachment.type === "task_reminder"` | `context.saved` | planning | `metadata.itemCount`, `metadata.content` |
| `attachment.type === "plan_mode"` | `context.saved` | planning | `metadata.planExists`, `planFilePath`, `isSubAgent`, `reminderType` |
| `attachment.type === "skill_listing"` | `instructions.loaded` | (default) | `metadata.skillCount`, body = listing text |
| `attachment.type === "deferred_tools_delta"` | `instructions.loaded` | (default) | `metadata.addedNames`, `removedNames` |
| `attachment.type === "mcp_instructions_delta"` | `instructions.loaded` | (default) | `metadata.addedNames`, `removedNames`, `addedBlocks` |
| `attachment.type === "nested_memory"` | `context.saved` | planning | `metadata.path`, `displayPath`, `memoryType`; body = memory content |
| `assistant.message.content[].type === "tool_use"` | skipped | — | already posted by PostToolUse hooks |
| `user.message.content[].type === "tool_result"` | skipped | — | implicit in hook success/failure |
| `attachment.type === "hook_success" / "async_hook_response" / "hook_additional_context" / "directory"` | skipped | — | already represented by hook events |
| `system.subtype === "local_command"` | skipped | — | out of scope for v1 |
| `permission-mode` | skipped | — | out of scope for v1 |

**Deterministic messageId (idempotency):**
```
messageId = sha1("transcript:" + sessionId + ":" + assistantUuid + ":" + contentIndex).slice(0, 32)
```
Replaying the same transcript range produces the same IDs so duplicate posts
are harmless on the server side.

**Tool-use correlation (`metadata.toolUseId`):**
Every PostToolUse handler plus `PostToolUseFailure.ts` now reads `tool_use_id`
from the hook payload and threads it into event metadata as `toolUseId`. The
same id lives on the transcript's `tool_use.id` block — the field that the
next-turn `thought.logged` event cites via `metadata.parentUuid` /
`assistantUuid` chaining — so downstream consumers can rebuild the
`thought → tool_use → tool_result` chain without guessing.

---

## 15. OTLP token collection — `adapter-otlp-logs`

Token usage is **not** read by the plugin hooks. Instead, Claude Code's built-in
OpenTelemetry exporter pushes per-API-call telemetry to the monitor's OTLP
endpoint, which converts it into `token.usage` timeline events.

### Activation (`.claude/settings.json`)

```json
{
  "env": {
    "CLAUDE_CODE_ENABLE_TELEMETRY": "1",
    "OTEL_LOGS_EXPORTER": "otlp",
    "OTEL_EXPORTER_OTLP_PROTOCOL": "http/json",
    "OTEL_EXPORTER_OTLP_LOGS_ENDPOINT": "http://127.0.0.1:3847/v1/logs",
    "OTEL_LOGS_EXPORT_INTERVAL": "5000"
  }
}
```

`CLAUDE_CODE_ENABLE_TELEMETRY=1` is the master switch — all other OTEL vars are
ignored without it. Requires a Claude Code restart to take effect.

### Data flow

```
Claude Code  ──OTLP HTTP/JSON──►  POST /v1/logs  (OtlpLogsController)
                                       │
                                       ▼
                               extractApiRequestRecords()
                               (filter: event.name === "api_request")
                                       │
                                       ▼
                        for each record: resolveRuntimeBinding(
                            "claude-plugin", record.sessionId
                        )
                                       │
                          ┌────────────┴─────────────┐
                     binding found               not found
                          │                          │
                          ▼                          ▼
                  logTokenUsage(...)             skipped: +1
                  accepted: +1
```

### Session → task routing

Each OTLP log record carries a `session.id` attribute (the Claude Code session
UUID). This is the same value that `SessionStart.ts` registers via
`/api/runtime-session-ensure`, so it directly maps to a `taskId` through the
`runtime_session_bindings` table:

```
OTLP attrs["session.id"]  =  runtimeSessionId
  └─► runtime_session_bindings(runtimeSource="claude-plugin", runtimeSessionId)
        └─► taskId  +  monitorSessionId
```

**Late-arrival fallback**: `SessionEnd` clears `monitorSessionId` in the binding
before the OTLP batch window (5 s) expires. `resolveRuntimeBinding` handles this
by falling back to `findTaskId()` + most-recent session lookup when the primary
`find()` (which filters `monitorSessionId IS NOT NULL`) returns null.

### OTLP attributes extracted (`event.name === "api_request"`)

| Attribute | Field | Notes |
|---|---|---|
| `session.id` | `sessionId` | Required — record skipped if absent |
| `input_tokens` | `inputTokens` | Defaults to 0 |
| `output_tokens` | `outputTokens` | Defaults to 0 |
| `cache_read_tokens` | `cacheReadTokens` | Defaults to 0 |
| `cache_creation_tokens` | `cacheCreateTokens` | Defaults to 0 |
| `cost_usd` | `costUsd` | Optional |
| `duration_ms` | `durationMs` | Optional |
| `model` | `model` | e.g. `"claude-sonnet-4-6"` |
| `prompt.id` | `promptId` | UUID identifying the API request; stored as metadata, not used for routing |

`session.id` falls back to the resource-level attribute if not present on the
individual log record.

### Resulting `token.usage` event

```
kind:  "token.usage"
lane:  "background"
title: "API call (<model>)"   or "API call" when model is absent
metadata:
  inputTokens        number
  outputTokens       number
  cacheReadTokens    number
  cacheCreateTokens  number
  costUsd            number?
  durationMs         number?
  model              string?
  promptId           string?   — Anthropic API request UUID (for future correlation)
  source             "otlp"
```

---

## File-system state summary

```
<PROJECT_DIR>/.claude/
├── .transcript-cursors/
│   ├── <sessionId>.json          # byte offset + lastEmittedUuid (set by Stop, deleted by SessionEnd)
│   └── sub--<agentId>.json       # subagent transcript cursor (set/cleared by SubagentStop)
└── .todo-state/
    └── <sessionId>.json          # TodoWrite state tracking (managed by lib/todo.state.ts)
```

Pre-v0.2.0 layouts (`.claude/.session-cache/` and `~/.claude/.session-history.json`)
are no longer written or read — see the migration note at the top of this
document. The only state still persisted across hook invocations is the
per-session transcript cursor.

---

## Hook directory structure

```
hooks/
├── util/                         # pure helpers: no I/O, no side effects
│   ├── paths.const.ts            # PROJECT_DIR, CLAUDE_RUNTIME_SOURCE, TRANSCRIPT_CURSOR_DIR
│   ├── paths.ts                  # defaultTaskTitle, relativeProjectPath
│   ├── payload.ts                # getToolInput, getSessionId, parseMcpToolName, …
│   ├── utils.ts                  # isRecord, toTrimmedString, ellipsize, createMessageId, …
│   └── json-file.store.ts        # atomic JSON read/write/delete
├── lib/                          # infrastructure: I/O, logging, HTTP transport
│   ├── hook/
│   │   ├── hook.context.ts       # readHookSessionContext, readToolHookContext
│   │   └── hook.log.ts           # hookLog, hookLogPayload
│   ├── transcript/
│   │   ├── transcript.cursor.ts  # .claude/.transcript-cursors/<sid>.json read/write
│   │   ├── transcript.emit.ts    # transcript entry → ingest event conversion
│   │   └── transcript.tail.ts    # incremental tail reader (uses transcript cursor)
│   └── transport/
│       └── transport.ts          # readStdinJson, postJson, postEvent, ensureRuntimeSession
├── Agent/
│   └── session.ts                # resolveEventSessionIds, resolveSubagentSessionIds, resolveBackgroundSessionIds
├── SessionStart.ts
├── UserPromptSubmit.ts
├── PreToolUse.ts
├── PostToolUse/                  # one file per tool matcher; add new matchers here
│   ├── Bash.ts
│   ├── File.ts                   # Edit | Write
│   ├── Explore.ts                # Read | Glob | Grep | WebSearch | WebFetch
│   ├── Agent.ts                  # Agent | Skill
│   ├── Todo/
│   │   ├── Todo.ts               # TaskCreate | TaskUpdate | TodoWrite
│   │   └── todo.state.ts         # TodoWrite state tracking
│   └── Mcp.ts                    # mcp__.*
├── PostToolUseFailure.ts
├── SubagentStart.ts
├── SubagentStop.ts
├── InstructionsLoaded.ts
├── PreCompact.ts
├── PostCompact.ts
├── SessionEnd.ts
└── Stop.ts
```

> `util/` and `lib/` are excluded from `tsconfig.json` direct analysis
> (`exclude: ["hooks/util", "hooks/lib"]`) because handlers import them directly
> as shared internal modules rather than compiling them as standalone hook
> entrypoints. Lane constants (`LANE`) and event kinds are imported from
> `../../shared/events/lanes.js` and `../../shared/events/kinds.js`. Semantic
> classification runs in the plugin via `../../shared/semantics/inference.*` —
> each handler calls the appropriate `infer*Semantic()` function and includes
> the result (`lane`, `subtypeKey`, `toolFamily`, etc.) in the ingest event payload.

---

## What the plugin never does

- Never writes to stdout (no `hookSpecificOutput`, no `additionalContext`)
- Never exits with code 2 (never blocks Claude Code)
- Never modifies tool input (`updatedInput`)
- All monitoring is purely observational — Claude Code's behavior is unaffected
