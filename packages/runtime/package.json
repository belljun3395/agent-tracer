{
  "name": "@monitor/runtime",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint:types": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.139"
  },
  "devDependencies": {
    "@monitor/kernel": "*",
    "esbuild": "^0.27.7",
    "@types/node": "^24.13.3",
    "typescript": "^5.9.2",
    "vitest": "^3.0.0"
  }
}
