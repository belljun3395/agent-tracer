{
  "name": "@monitor/runtime-claude",
  "version": "0.2.1",
  "private": true,
  "description": "Claude Code runtime adapter that streams hook events to the Agent Tracer monitor server.",
  "type": "module",
  "scripts": {
    "lint": "eslint hooks && npm run lint:types",
    "lint:types": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "tsx": "^4.0.0"
  }
}
