/** 대화 챗의 HTTP 계약이며 웹 클라이언트와 서버 컨트롤러가 같은 값을 읽는다. */
export const CHAT_THREADS_PATH = "/api/v1/chat/threads";

/** 사용자 장기기억의 HTTP 계약이며 LangGraph 백엔드의 저장소가 사용자 범위로 읽고 쓴다. */
export const CHAT_MEMORY_PATH = "/api/v1/chat/memory";
