{
    "name": "@monitor/ws-gateway",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "lint": "eslint . && npm run lint:types",
        "lint:types": "tsc -p tsconfig.json --noEmit",
        "lint:fix": "eslint . --fix"
    },
    "exports": {
        "./*": "./src/*"
    },
    "dependencies": {
        "@monitor/shared": "*",
        "redis": "^5.12.1",
        "ws": "^8.0.0"
    },
    "devDependencies": {
        "@types/ws": "^8.0.0",
        "vitest": "^3.0.0"
    }
}
