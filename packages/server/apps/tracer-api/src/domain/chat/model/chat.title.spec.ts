/** 새 스레드가 시작하는 기본 제목이며, 스레드 제목이 이 값인 동안에만 첫 성공한 턴 뒤 자동 제목 생성이 실행된다. */
export const CHAT_DEFAULT_THREAD_TITLE = "New conversation";

/** 생성된 제목을 이 길이로 자른다. */
export const CHAT_TITLE_MAX_LENGTH = 40;
