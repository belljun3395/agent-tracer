/**
 * DI token for the local embedding service. Used internally by the event
 * module's search adapter; not exported as part of event.public.
 */
export const EMBEDDING_SERVICE_TOKEN = "EVENT_EMBEDDING_SERVICE";
