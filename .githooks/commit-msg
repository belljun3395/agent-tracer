#!/bin/sh
# 커밋 메시지 규칙과 공동 작성자 허용 목록을 로컬에서 집행한다.
ROOT="$(git rev-parse --show-toplevel)"
node "$ROOT/scripts/check-commit-msg.mjs" "$1" || exit 1
node "$ROOT/scripts/check-coauthors.mjs" --file "$1" || exit 1
